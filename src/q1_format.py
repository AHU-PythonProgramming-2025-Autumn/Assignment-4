"""
出题者：柳文章，安徽大学人工智能学院
出题日期：2025.11.1

任务：编写函数 `format_score_card(name, course, score)`

- 使用字符串格式化生成成绩单；
- 成绩保留两位小数；
- 若成绩 < 60，输出“未通过”；
  若成绩 ≥ 90，输出“优秀”；
  若成绩 <90 且 >=80，输出“良好”；
  若成绩 <80 且 >= 60 输出“合格”；
  否则输出“非法成绩”。
"""


def format_score_card(name: str, course: str, score: float) -> str:
    """
    使用字符串格式化输出成绩单。
    - 成绩保留两位小数
    - 根据成绩显示等级

    示例输出：
         ```
         学生成绩单
         --------------------------
         姓名：Alice
         课程：Python编程
         成绩：95.68分（优秀）
         --------------------------
         ```
    """
    # TODO:


if __name__ == "__main__":
    print(format_score_card("张三", "Python", 95.68))
